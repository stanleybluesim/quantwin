[pytest]
testpaths = tests
norecursedirs =
    .git
    .venv
    .venv-strictgate
    .pytest_cache
    .ruff_cache
    artifacts
    quantwin_phase0_snapshot
addopts = -q --maxfail=1
markers =
    e2e: end-to-end tests
