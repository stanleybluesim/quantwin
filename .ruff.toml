exclude = [
  ".git",
  ".venv",
  ".venv-strictgate",
  ".pytest_cache",
  ".ruff_cache",
  "artifacts",
  "quantwin_phase0_snapshot",
]

[lint.per-file-ignores]
"tests/test_e2e_golden_thread.py" = ["E402"]
